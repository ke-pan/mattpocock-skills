---
"mattpocock-skills": patch
---

Teach `/implement` to carry known ticket and spec provenance into its final commit without making a ticket mandatory.

Completed tracker items receive the configured tracker's closing reference, while broader parent specs are mentioned without being closed. Local spec paths are preserved as plain text, and conversation-only work still commits normally without inventing an identifier. The GitHub, GitLab, and local tracker templates now define the corresponding commit conventions.
